mutation AccountRequestDeletionMutation(
  $channel: String
  $redirectUrl: String!
) {
  accountRequestDeletion(channel: $channel, redirectUrl: $redirectUrl) {
    errors {
      ...AccountErrorFragment
    }
  }
}
