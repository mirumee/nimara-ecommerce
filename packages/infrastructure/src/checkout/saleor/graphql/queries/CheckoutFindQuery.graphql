query CheckoutFindQuery(
  $checkoutId: ID!
  $countryCode: CountryCode!
  $languageCode: LanguageCodeEnum!
  $thumbnailSize: Int!
  $thumbnailFormat: ThumbnailFormatEnum!
  $includeVendorMetadata: Boolean!
) {
  checkout(id: $checkoutId) {
    ...CheckoutFragment
  }
}
