fragment ProductBaseFragment on Product {
  id
  name
  seoTitle
  description
  seoDescription
  category {
    name
    slug
  }
  translation(languageCode: $languageCode) {
    name
    description
  }
}
