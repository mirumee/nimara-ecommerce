query StoredPaymentMethodListQuery($channel: String!) {
  me {
    storedPaymentMethods(channel: $channel) {
      id
      paymentMethodId
      name
      type
      data
      creditCardInfo {
        brand
        firstDigits
        lastDigits
        expMonth
        expYear
      }
    }
  }
}
