mutation TransactionProcessMutation($id: ID!, $data: JSON) {
  transactionProcess(id: $id, data: $data) {
    transaction {
      id
      order {
        id
      }
    }
    transactionEvent {
      id
      type
      message
    }
    data
    errors {
      field
      message
      code
    }
  }
}
