query Page($slug: String!, $languageCode: LanguageCodeEnum!) {
  page(slug: $slug) {
    id
    pageType {
      slug
    }
    attributes {
      attribute {
        ...AttributeFragment
      }
      values {
        ...AttributeValueFragment
      }
    }
    title
    content
  }
}
