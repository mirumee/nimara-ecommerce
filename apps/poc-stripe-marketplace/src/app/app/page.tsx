export default function AppPage() {
  return (
    <main
      style={{
        margin: "40px auto",
        maxWidth: "720px",
        padding: "0 16px",
        fontFamily: "system-ui, sans-serif",
      }}
    >
      <h1>POC Stripe Marketplace API App</h1>
      <p>This app is installed. Use REST endpoints under /api/saleor.</p>
    </main>
  );
}
