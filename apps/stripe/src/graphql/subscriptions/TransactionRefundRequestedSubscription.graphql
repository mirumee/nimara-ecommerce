subscription TransactionRefundRequestedSubscription {
  event {
    ... on TransactionRefundRequested {
      recipient {
        ...PaymentGatewayRecipientFragment
      }
      action {
        ...TransactionActionFragment
      }
      # transaction {
      #   ...OrderOrCheckoutFragment
      # }
    }
  }
}
