query CustomersByIds($ids: [ID!]!, $first: Int) {
  customers(first: $first, filter: { ids: $ids }) {
    edges {
      node {
        id
        firstName
        lastName
        email
      }
    }
  }
}
