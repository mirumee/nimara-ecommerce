query VendorPageStatus($id: ID!) {
  page(id: $id) {
    id
    title
    slug
    attributes {
      attribute {
        id
        slug
      }
      values {
        name
        file {
          url
        }
      }
    }
  }
}
