mutation AddOrderNote($order: ID!, $input: OrderNoteInput!) {
  orderNoteAdd(order: $order, input: $input) {
    errors {
      field
      message
      code
    }
    order {
      id
    }
    event {
      id
      message
      date
    }
  }
}
