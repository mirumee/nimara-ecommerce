mutation DraftOrderUpdate($id: ID!, $input: DraftOrderInput!) {
  draftOrderUpdate(id: $id, input: $input) {
    order {
      id
      number
      status
    }
    errors {
      field
      message
      code
    }
  }
}

