mutation OrderUpdateMutation($id: ID!, $input: OrderUpdateInput!) {
  orderUpdate(id: $id, input: $input) {
    order {
      id
      number
      status
    }
    errors {
      field
      message
      code
    }
  }
}

