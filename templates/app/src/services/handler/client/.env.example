# Local config UI (dev only). Set both to open /app outside the Saleor
# Dashboard; the server reads the token too, to reach a Saleor the app is not
# installed on yet.
VITE_SALEOR_API_URL=
VITE_SALEOR_APP_TOKEN=
